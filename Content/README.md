# Content

Write thesis chapters and section snippets here.

Use `\include{Content/ChapterName}` for chapter-sized files from `Thesis.tex`.
Use `\input{Content/SectionName}` for smaller section snippets inside a chapter.
